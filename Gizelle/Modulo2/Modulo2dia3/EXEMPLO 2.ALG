Algoritmo "exemplo 2"

//ESTRUTURA DE REPETIÇÃO COM INTERRUPÇÃO NO FIM
//algoritmo "RepeticaoInterrupcaoFim"


Var

count : inteiro

inicio

      count <- 0
      repita
            Escreval(count)
            count <- count + 1
      ate count > 100

fimalgoritmo